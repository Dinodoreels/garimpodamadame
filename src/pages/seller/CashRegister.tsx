// Seller version - simplified cash register (same as admin but already scoped by RLS)
import CashRegisterPage from '@/pages/admin/CashRegister';

export default function SellerCashRegister() {
  return <CashRegisterPage />;
}
